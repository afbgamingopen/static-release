server {
        listen  80;
        server_name  dash.afbok.com m.afbok.com nm.afbok.com;
        root /www/static/release/lgm-prod/web;

        location = /index.html {
            expires 5m;
        }

        location / {
            expires 7d;
        }
}
