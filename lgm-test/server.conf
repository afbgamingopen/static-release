server {
        listen  80;
        server_name  testdash.afbok.com;
        root /www/static/release/lgm-test/web;

        location = /index.html {
            expires 5m;
        }

        location / {
            expires 7d;
        }
}
